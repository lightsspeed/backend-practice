# This is slice3.py

# What is the purpose of the yield keyword in a generator function?

# Answer: yield pauses the function and returns a value, saving the state to resume later; e.g., yield x in def gen(): yield 1 produces values one at a time.

